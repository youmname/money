你是我的全栈开发助手（Vue3 + Vite + Node Express + MySQL Prisma）。
请严格遵守我项目的目录与规范：

目录规范：
- 页面：src/views（按 teacher/student/parent 分目录）
- 可复用组件：src/components（按 base/common/classroom 分目录）
- API调用：src/api（页面只能调用api函数拿数据，禁止写死数组）
- 常量/枚举：src/constants

开发规则（必须遵守）：
1) PC优先自适应iPad/手机；家长端H5只读
2) 每一行代码添加中文注释（变量作用、元素用途、函数输入输出）
3) 输出格式必须包含：
   - 修改/新增文件清单
   - 每个文件完整代码
   - 验收步骤（我运行后应该看到什么）
4) 不引入新库，除非我明确允许，并解释引入原因
5) 只做P0范围，不要提前做P1/P2

【今天任务】：
（我会在这里写今天要做哪个页面/组件/接口）

【验收标准】：
（我会列3条肉眼可检查的结果）
